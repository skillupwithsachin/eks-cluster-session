apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: nginx-pvc
  namespace: k8s-demo

spec:

  accessModes:
    - ReadWriteOnce

  storageClassName: gp3

  resources:

    requests:
      storage: 5Gi
