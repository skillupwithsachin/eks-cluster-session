apiVersion: v1
kind: ConfigMap

metadata:
  name: app-config
  namespace: k8s-demo

data:

  APP_ENV: "dev"
  APP_NAME: "eks-demo"
  LOG_LEVEL: "INFO"

  application.properties: |
    server.port=8080
    application.name=eks-demo
